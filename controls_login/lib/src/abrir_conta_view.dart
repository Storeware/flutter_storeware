// @dart=2.12

import 'package:flutter/material.dart';
//import 'package:url_launcher/url_launcher.dart';

class AbrirContaView extends StatelessWidget {
  const AbrirContaView({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
        // body: launch ('https://wbagestao.com',enableJavaScript: true) ,
        );
  }
}
